def missing_char(str, n):
    length = len(str)
    if n > 0:
      tempstart = str[:n]
      tempend = str[n+1:]
      return tempstart + tempend
    if n == 0:
      tempstart = str[1:] 
      return tempstart
    if n == length:
      tempend = str[:n]
      return tempend
